version: '3.8'
services:
  api:
    build: ./ZorgtechnologieProduct.API
    ports:
      - "5000:80"
    environment:
      - ConnectionStrings__DefaultConnection=${ConnectionStrings__DefaultConnection}
      - AzureServiceBus__ConnectionString=${SERVICEBUS_CONNECTION}
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
