services:
  web:
    build:
      context: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=advice
      - FLASK_ENV=development
    command: bash -c "conda run -n advice flask run --host=0.0.0.0 --port=5000"
